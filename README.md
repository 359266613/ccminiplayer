# CCMiniPlayer

Control Center mini Now Playing UI for iOS 16.5 (Dopamine rootless).

## Build

```bash
export THEOS=~/theos
make package
```

GitHub Actions will build a `.deb` in `packages/` and upload it as an artifact.
